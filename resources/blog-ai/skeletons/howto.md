# Skeleton — howto

Fixed slot order (do not reorder):

1. `quick_answer` — SEO block দ্রুত উত্তর (not an H2 in outline sections)
2. `intro_scene` — H2: seller problem / why this matters today in BD COD
3. `core_teaching` — H2: what to understand before doing the steps
4. `steps` — H2: numbered practical steps (the only required numbered list)
5. `differentiation` — H2: common mistakes / vs generic advice / competitor gaps
6. `tool_bridge` — H2 or short section: when WooEasyLife helps + must_link
7. `ai_summary` — SEO block এআই সারাংশ
8. `faqs` — 5–7 spoken Q&A
9. `cta_close` — soft closing CTA to primary_path

Outline `sections` should cover slots 2–6 (each with `"slot": "..."`).
Word targets: intro ~150, core ~250, steps ~400, differentiation ~250, tool_bridge ~150.
