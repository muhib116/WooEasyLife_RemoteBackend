# Skeleton — comparison

Fixed slot order (do not reorder):

1. `quick_answer` — SEO block দ্রুত উত্তর
2. `intro_scene` — H2: why sellers compare these options in BD
3. `comparison` — H2: side-by-side (include an HTML table of real options)
4. `when_to_choose` — H2: which path fits which seller situation
5. `differentiation` — H2: what generic blogs miss (competitor gaps)
6. `tool_bridge` — soft WooEasyLife + must_link
7. `ai_summary` — SEO block এআই সারাংশ
8. `faqs` — 5–7 spoken Q&A
9. `cta_close` — soft CTA to primary_path

Outline `sections` should cover slots 2–6 (each with `"slot": "..."`).
Never invent metrics in the table — only product_brief / landing claims.
