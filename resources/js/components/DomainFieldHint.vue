<template>
    <p class="text-xs text-gray-500 dark:text-gray-400">
        Enter the store hostname only (e.g.
        <code class="rounded bg-slate-100 px-1 dark:bg-slate-800">shop.example.com</code>
        or
        <code class="rounded bg-slate-100 px-1 dark:bg-slate-800">localhost</code>).
        Full URLs are normalized automatically — port and path are ignored.
    </p>
</template>
