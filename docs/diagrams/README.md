# Diagrams

Architecture and engineering diagrams referenced by `architecture/` or `implementation/`.

Prefer stable filenames (`kebab-case`). Diagrams must not invent modules or artifacts absent from Reference Architecture 1.0.0.
