# Runtime specifications

Describe how each frozen module behaves in production.

**Constraint:** Behavior must preserve Document `002` ownership and Execution Boundary. No new runtime concepts beyond Documents `001`–`011`.

**Typical contents (when authored):** Context First path, DecisionResult publication, Adapter application of ExecutionRequest, observational EvaluationResult.
