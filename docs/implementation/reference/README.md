# Reference implementation

Notes and pointers for code that conforms to Reference Architecture 1.0.0.

**Constraint:** Reference builds prove contracts; they do not redefine them. Prefer Migration Phase order (Document `011`) over greenfield rewrites.
